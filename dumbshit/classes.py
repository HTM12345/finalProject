import time

main: 
words = ['heklajdfkljaksjdfjas', 'fahds']

wordsCounter = 0

listClass = []

for i in words:
    listClass.append(Words(i))

while gameLoop:
    if word.checkCounter:
        


class Words(word):
    def __init__(self):
        self.time = 0
        self.accuracy = 0
        self.wordLen = len(str(word))
        self.letterList = [i for i in str(word)]
        self.counter = 0
        self.letter = letterList[counter]
        self.checkCounter = checkCounter()

    def checkCounter(self):
        if self.wordLen == counter:
            return True
        
    def checkCorrect(self, letterPressed):
        if self.letter == letterPressed:
            counter += 1
            return self.checkCounter()
    
    
        
